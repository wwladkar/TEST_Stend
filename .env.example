# Обязательная переменная — секрет для подписи JWT (минимум 32 байта)
JWT_SECRET=your-random-secret-at-least-32-bytes-long!!

# Время жизни JWT в миллисекундах (по умолчанию 24 часа)
JWT_EXPIRATION_MS=86400000
