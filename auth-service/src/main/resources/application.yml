server:
  port: 8081

spring:
  datasource:
    url: jdbc:h2:file:C:/Users/User/IdeaProjects/TEST_Stend/data/auth-db
    username: sa
    password:
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

jwt:
  secret: 
  expiration-ms: 86400000
